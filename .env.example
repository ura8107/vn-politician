# Data lives in Cloudflare D1, so no database credentials are required.
# The only optional build-time value is the production URL used in metadata.

# Optional: production URL used for metadata (OG images, sitemap, etc.).
# Leave unset to fall back to http://localhost:3000.
NEXT_PUBLIC_APP_URL=https://vn-politician.<YOUR-ACCOUNT>.workers.dev